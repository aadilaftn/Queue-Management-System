# AWS IoT Core Configuration
# To enable MQTT integration, set these environment variables

# Your AWS IoT endpoint (e.g., aqrvs9wjclzg2-ats.iot.us-east-1.amazonaws.com)
# Get it from: aws iot describe-endpoint --endpoint-type iot:Data-ATS
IOT_ENDPOINT=your-endpoint.iot.region.amazonaws.com

# Path to device certificate (mTLS)
IOT_CERT_PATH=./spider.cert.pem

# Path to device private key (mTLS)
IOT_KEY_PATH=./spider.private.key

# Path to root CA certificate (optional, for certificate verification)
IOT_CA_PATH=./root-CA.crt

# MQTT topic prefix for this clinic
# Format: queue/clinic/{clinicId}/
# Messages are published to: queue/clinic/{clinicId}/updates
# Server subscribes to: queue/clinic/{clinicId}/incoming/+
IOT_TOPIC_PREFIX=queue/clinic/default/

# MQTT client ID (should be unique per server instance)
MQTT_CLIENT_ID=queue-server-main

# DynamoDB Configuration (Optional)
# DYNAMODB_TABLE=QueueEntries
# AWS_REGION=us-east-1
# DYNAMO_ENABLE=true

# Server Port
# PORT=3000

# Average service time in seconds (default 180 = 3 minutes)
# AVG_SERVICE_SECONDS=180

# Clinic ID (defaults to random UUID, persisted in data/clinic_id.txt)
# CLINIC_ID=clinic-123

# Admin secret for web-based token creation
# SECRET_ADMIN_TOKEN=your-secure-secret

# Allow web-based token creation without admin secret (not recommended for production)
# ALLOW_WEB_TOKENS=false
